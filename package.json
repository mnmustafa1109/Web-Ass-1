{
  "name": "web-assignment-1",
  "version": "0.0.1",
  "description": "",
  "keywords": ["assignment"],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html && parcel build img/*",
    "dev": "npm run build-img && parcel index.html --open",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-img": "parcel build img/* --no-cache"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  },
  "trustedDependencies": ["parcel"],
  "dependencies": {
    "express": "^4.18.2",
    "jquery": "^3.7.1"
  }
}
